\doxysection{std\+::mutex}
\hypertarget{std_1_1mutex-example}{}\label{std_1_1mutex-example}status\+Change\+Mutex

status\+Change\+Mutex
\begin{DoxyParams}{Parameters}
{\em p\+DQ} & reference to the DispatchQ object \\
\hline
{\em p\+Name} & name of the mutex \\
\hline
{\em p\+Thread\+Mutex\+State} & state of the thread mutex (NEED, HAVE, FREE)\\
\hline
\end{DoxyParams}
This function is used to change the state of the thread mutex. It is used to log when a thread is waiting or handling a mutex. mtx; // Simple mutex Mutex\+Need(p\+DQ, "{}mtx"{}); mtx.\+lock(); // Lock the mutex Mutex\+Have(p\+DQ, "{}mtx"{}); // ... Critical Section ... Mutex\+Free(p\+DQ, "{}mtx"{}); mtx.\+unlock(); // Déverrouille le mutex \begin{DoxyWarning}{Warning}
The user must ensure that p\+Name is unique for each mutex, and that the exact same name is consistently used across all three status\+Change\+Mutex instances associated with a single mutex
\end{DoxyWarning}

\begin{DoxyCodeInclude}{0}

\end{DoxyCodeInclude}
 